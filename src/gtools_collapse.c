#define RADIX_SHIFT 16
#include "gtools_collapse.h"

int sf_collapse (
    size_t k1,
    size_t k2,
    size_t in1,
    size_t in2,
    size_t index[],
    size_t info[],
    size_t *J,
    int verbose)
{

    // char *pt;
    // pt = strtok (str,",");
    // while (pt != NULL) {
    //     int a = atoi(pt);
    //     printf("%d\n", a);
    //     pt = strtok (NULL, ",");
    // }

    return (0);
}
