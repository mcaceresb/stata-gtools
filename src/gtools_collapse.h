#ifndef GTOOLS_COLLAPSE
#define GTOOLS_COLLAPSE

int sf_collapse (
    size_t k1,
    size_t k2,
    size_t in1,
    size_t in2,
    size_t info[],
    size_t index[],
    size_t *J,
    int verbose
);

#endif
